# gotchas — delegate-first 정본 레포

실패 직후 즉시 기록한다. 증상 / 원인 / 복구 / 재발 방지 각 한 줄.
작업 착수 시 이 파일을 **강제 리리드**한다 — 기억이 아니라 파일 기준으로 시작한다.

---

## 2026-08-18 — 머지 실패인데 브랜치를 지워 커밋이 고아가 됨

- **증상**: PR#5 머지가 GitHub 503으로 실패했는데 같은 명령줄의 후속 정리 단계가 그대로 실행돼 원격·로컬 브랜치가 둘 다 삭제됐다. 커밋 2개(`5af94c4`, `650db70`)가 어느 브랜치에도 속하지 않는 고아가 되고 PR은 CLOSED로 전환됐다.
- **원인**: `gh pr merge ... ; git push origin --delete <branch> ; git branch -D <branch>` 를 **머지 성공 여부와 무관하게** 한 줄에 이어 붙였다. `&&`가 아니라 `;`/개행으로 연결해 실패가 전파되지 않았다.
- **복구**: 로컬 객체는 살아 있었다 — `git reflog`로 SHA를 찾아 `git branch <name> <sha>`로 브랜치를 되살리고, push → `gh pr reopen` → 재머지. 손실 0.
- **재발 방지**: 브랜치 삭제는 **머지 확인 후 별도 단계**로만 한다. `gh pr view <n> --json state -q .state` 가 `MERGED`인지, 또는 `git log main | grep <sha>`로 커밋이 main에 들어갔는지 확인한 **뒤에** 지운다. 정리 명령을 머지 명령과 같은 배치에 넣지 않는다.

## 2026-08-18 — 커밋 메시지 안의 중첩 따옴표가 커밋을 조용히 깨뜨림

- **증상**: `git commit -m "... \"'날짜' 포함 ...\" ..."` 에서 큰따옴표가 인자를 조기 종료해 커밋이 실패했는데, 이어진 `git push`가 **이전 커밋을 밀며 "pushed"를 출력**해 성공처럼 보였다.
- **원인**: 한국어 본문에 인용부호를 쓰면서 `-m`을 셸 큰따옴표로 감쌌다.
- **재발 방지**: 여러 줄·인용부호가 있는 커밋 메시지는 **파일로 넘긴다**(`git commit -F <file>`). 커밋 직후 `git log --oneline -1`로 실제로 새 커밋이 생겼는지 확인한다.

## 2026-08-18 — `git status`가 무침해의 증거가 아니었다

- **증상**: "원본 프로젝트를 건드리지 않았다"를 `git -C <repo> status --porcelain` 0행으로 증명하려 했는데, 그 레포의 `.git/info/exclude`가 `.claude/skills/`·`.claude/agents/`를 제외하고 있어 **그 트리는 애초에 status에 뜨지 않았다**.
- **재발 방지**: 무변경 주장은 **내용 대조**(`cmp`/해시)로 한다. 추적 제외 경로에서는 status·diff가 침묵한다 — 침묵을 증거로 쓰지 않는다.

## 2026-08-18 — 오탐을 고치다 미탐을 만들었다 (양방향 테스트 없이 좁히지 말 것)

- **증상**: 린터의 표 헤더 판정을 부분문자열 → 전체 일치로 좁혀 오탐을 없앴는데, 그 결과 **헤더가 깨진 표는 "헤더 아님"으로 분류돼 표 전체가 검사에서 빠졌다**. 위반 3건이 든 로그가 `위반 없음 / exit 0`.
- **원인**: 판정 조건을 좁히면서 "좁힌 쪽으로 빠져나가는 입력"을 검증하는 **양성 케이스를 만들지 않았다**. 추가한 것은 오탐을 막는 음성 케이스뿐이었다.
- **재발 방지**: 검사 조건을 **좁힐 때는 반드시 양성 케이스를 같이 추가**한다("이걸 좁혀도 여전히 잡히는가"). 그리고 테스트의 **비공허성**을 매번 확인한다 — 수정 전 코드로 돌려 그 케이스가 실패하는지 본다.
