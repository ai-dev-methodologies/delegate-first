# delegate-first 스킬 이식 패키지 (handoff)

- 작성일: 2026-08-17
- **명칭 확정(2026-08-17)**: 사용자 결정으로 `delegating-execution` → **`delegate-first`** 개명. 이식 시 새 레포명·스킬 디렉토리·슬래시 명령 모두 delegate-first 기준.
- 출처: goone-rest 로컬 `.claude/skills/delegate-first/` (의도적으로 git 추적 대상 밖 — untracked)
- 목적: `/delegate-first` 스킬을 별도 GitHub 레포로 독립시켜 관리하기 위한 이식 패키지. **이 파일 하나만 새 레포 세션에 전달하면 스킬 전체를 재구성·설치할 수 있다.**

---

## 1. 이 패키지가 무엇인가

`/delegate-first` 스킬은 메인 세션(고비용 프론트티어 모델)이 실행 작업을 직접 하지 않고 계획·위임·리뷰·판정만 하도록 강제하는 워크플로 규율이다. 스킬 본문(`SKILL.md`)이 5단계 체크리스트(분해→라우팅→위임→리뷰→판정)로 위임을 안내하고, `references/` 2종이 "어떤 작업에 어떤 모델·effort·실행경로"(`routing-matrix.md`)와 "어떤 프롬프트 템플릿"(`prompt-templates.md`)을 쓸지 정한다. tier 에이전트 5종(`.claude/agents/*.md`)이 라우팅 매트릭스가 가리키는 model+effort 조합을 frontmatter에 미리 고정해두고, PreToolUse 훅(`enforce-subagent-model.cjs`)이 model을 지정하지 않은 Agent 호출을 기계적으로 차단해 스킬의 원칙을 강제한다. 글로벌 규칙 파일(`subagent-model-routing.md`)이 이 훅의 정책 근거를 문서로 남긴다.

구성 요소 관계도:

```
SKILL.md (원칙 + 5단계 체크리스트)
  ├─ references/routing-matrix.md   (작업유형 → model → effort → 실행경로 → tier에이전트)
  ├─ references/prompt-templates.md (조사용 / 구현용 프롬프트 템플릿 2종)
  └─ tier 에이전트 5종 (.claude/agents/*.md)
        explorer-low(haiku/low, 읽기전용) · executor-med(sonnet/medium)
        · executor-high(sonnet/high) · reviewer-high(opus/high, Write/Edit 제외)
        · judge-max(fable/max, Write/Edit 제외)
              ↑ routing-matrix.md가 이 5종의 model+effort 조합을 가리킴
              ↑ 강제(기계적 차단)
        PreToolUse 훅: enforce-subagent-model.cjs
              (matcher: Agent — tool_input.model 없으면 exit 2로 차단)
              ↑ 정책 근거
        글로벌 규칙: subagent-model-routing.md
              (전 프로젝트 공통 규칙, 2026-07-16 사용자 확정)
```

---

## 2. 새 레포 권장 구조

```
<new-repo>/
├── .claude/
│   ├── skills/
│   │   └── delegate-first/
│   │       ├── SKILL.md
│   │       └── references/
│   │           ├── routing-matrix.md
│   │           └── prompt-templates.md
│   ├── agents/
│   │   ├── explorer-low.md
│   │   ├── executor-med.md
│   │   ├── executor-high.md
│   │   ├── reviewer-high.md
│   │   └── judge-max.md
│   ├── hooks/
│   │   └── enforce-subagent-model.cjs
│   └── rules/
│       └── subagent-model-routing.md   (선택 — 프로젝트 로컬 규칙으로 두고 싶을 때)
└── docs/handoff/delegation-log.md      (SKILL.md 3단계가 요구하는 위임 로그 — 신규 생성 필요)
```

### 설치 방법 (택1)

**A) 프로젝트 로컬 설치 (권장, 빠름)** — 위 디렉토리 구조 그대로 새 레포의 `.claude/` 아래 복사한다. 훅은 `settings.json`(또는 `settings.local.json`)의 `PreToolUse`에 `matcher: "Agent"`로 등록해야 실제로 작동한다:

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          { "type": "command", "command": "node <훅경로>/enforce-subagent-model.cjs" }
        ]
      }
    ]
  }
}
```

goone-rest 원본은 이 훅을 `~/.claude/hooks/`(사용자 홈, **전역**)에 두고 전 프로젝트 공통으로 강제한다. 새 레포에서도 전역 설치를 유지할지, 레포 로컬 `.claude/hooks/`로 옮길지는 선택 사항이다 — 로컬로 옮기면 위 `command` 경로만 그에 맞게 바꾸면 된다.

**B) 플러그인화** — skill + agents + hook을 하나의 Claude Code 플러그인 매니페스트로 묶어 여러 레포에서 재사용. 이 handoff 시점(2026-08-17)에는 아직 플러그인화되지 않았다 — goone-rest에는 로컬 `.claude/` 트리로만 존재한다. 여러 레포에서 쓸 계획이면 새 레포에서 플러그인 매니페스트 작성을 첫 작업으로 고려할 것.

---

## 3. 파일 전문

### 파일: `.claude/skills/delegate-first/SKILL.md`

````markdown
---
name: delegate-first
description: Enforces that the main session acts only as planner, delegator, reviewer, and judge — routing all execution work (edits, writes, execution-style commands) to subagents with a task-appropriate model and reasoning effort. Use ONLY when the user explicitly invokes this skill by name (/delegate-first) or explicitly asks for the delegation discipline. Do not activate proactively.
---

# Delegate First

> Explicit-invocation only — do not self-trigger on task shape. Apply this discipline only when the user names this skill or explicitly asks for it.

## Principle

The main session runs a high-cost frontier model. Its job is planning, delegating, reviewing, and judging — not execution.

Before any Edit, Write, or execution-style Bash call, ask: **"Can this be delegated?"**
The only exceptions are a one-off read for context and a single verification command run to confirm a subagent's claim.

## Main-session reasoning-effort policy

The main session's model and reasoning-effort *setting* are the user's call (session config) — this skill does not set them. What this skill governs is **reasoning discipline within a turn**: how hard the main session should think before acting, per turn type.

| Turn type | Reasoning depth |
|---|---|
| Mechanical relay, status check, or idle-notification response | Minimal — answer immediately |
| Review of a subagent's output | Medium |
| Judgment, design, or escalation diagnosis | Deep |

Delegated effort is a separate matter, decided by [references/routing-matrix.md](references/routing-matrix.md) — not by this section. The execution path determines whether effort can even be specified: Workflow `agent()`'s `effort` param, `codex exec`'s `model_reasoning_effort`, or (for the Agent tool) not at all — see routing-matrix.md for details.

## Flow checklist

Copy this into your response and check off as you go:

```
Delegation Progress:
- [ ] 1. Decompose the task and classify its type
- [ ] 2. Pick model + effort + execution path (routing-matrix.md)
- [ ] 3. Delegate with a scoped prompt (prompt-templates.md) — and log it: agent/role/model/effort/path appended to the project delegation log (docs/handoff/delegation-log.md); effort not specifiable on ad-hoc Agent calls → record "(default)"
- [ ] 4. Review: don't trust self-report — reproduce key claims (grep/run)
- [ ] 5. Judge: pass / re-delegate (strengthen prompt) / escalate
```

**Step 1 — Decompose & classify.** Split the task into units of one type each: exploration/extraction, doc editing/structuring, general implementation, first-pass review, adversarial verification/judgment, or architecture/legal/money/authorization.

**Step 2 — Route.** See [references/routing-matrix.md](references/routing-matrix.md) for model, effort, and execution path per task type.

**Step 3 — Delegate.** See [references/prompt-templates.md](references/prompt-templates.md) for the two standard templates (read-only investigation, implementation/edit). Every delegation prompt must state: role in one line, working directory + boundaries (what must not be touched), forbidden system-level commands, required evidence (an "it doesn't exist" claim must state the search scope), a completion checklist, and the expected output format.

**Step 4 — Review.** A subagent's self-report is not evidence on its own. Reproduce its central claim yourself — grep the file it says it edited, run the test it says passes, read the diff it says exists.

**Step 5 — Judge.** Pass and move on, re-delegate with a strengthened prompt, or escalate per the ladder below.

## Escalation ladder

When a subagent's output fails review, diagnose the cause before retrying:

| Cause | Fix |
|---|---|
| Instructions unclear | Same model, strengthen the prompt |
| Insufficient capability | Raise model and/or effort |
| Scope too large | Split into smaller units |

Always strengthen the prompt alongside any model upgrade — raising the model without fixing the prompt just repeats the same failure at higher cost.

- Same root cause fails twice → escalate model/effort unconditionally on the retry.
- Fails a 3rd time → stop delegating, report the blocker to the user.

## Mandatory top-tier-model (fable) triggers

The main session's own model is a user setting (session config) — it may be `fable`, `opus`, or anything else. This skill does not assume which. Regardless of the main session's model, these 5 judgment types must go through the top-tier model (`fable`): if the main session already is `fable`, it performs them directly; otherwise, it delegates to a `fable` subagent.

1. ADR-level decisions
2. Shipping/release gates
3. Adversarial verification
4. Final-instance escalation review
5. Legal, financial, permission/authorization, or security judgment calls

## Prohibited

Do not auto-recalibrate the routing matrix from delegation logs. Routing stays principle-based — update [references/routing-matrix.md](references/routing-matrix.md) by hand, immediately, when a routing choice turns out wrong. For feedback-loop design beyond what's in this skill, see the Anthropic Agent Skills best-practices guide: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
````

### 파일: `.claude/skills/delegate-first/references/routing-matrix.md`

````markdown
# Routing Matrix

## Contents
- Task type → model → effort → execution path → tier agent
- Execution path constraints (Agent tool / Workflow `agent()` / `codex exec`)
- Fallback rules for cases not covered by the table

## Table

| 작업 유형 | 권장 모델 | effort | 실행 경로 | tier 에이전트 |
|---|---|---|---|---|
| 탐색·추출 (파일 검색, grep, 단순 집계) | haiku / sonnet | low~medium | Agent 툴 | `explorer-low` |
| 문서 편집·구조화 | sonnet | medium | Agent 툴 | `executor-med` |
| 일반 구현 | sonnet | medium~high | Agent 툴 또는 Workflow `agent()` | `executor-med`(medium) / `executor-high`(high) |
| 리뷰 1차 | sonnet | medium | Agent 툴 | 정확 매치 없음 — 낮은 리스크면 `executor-med`로 첫 패스, 결과가 걸리는 리뷰면 `reviewer-high`로 상향 |
| 적대 검증·판정 | opus / fable | high~max | Workflow `agent()` (effort 필요) 또는 Agent 툴(tier 에이전트 사용 시) | `reviewer-high`(opus) / `judge-max`(fable, 5종 트리거 해당 시) |
| 아키텍처·법·돈·권한·보안 | fable | max | Workflow `agent()` 또는 Agent 툴(tier 에이전트 사용 시) | `judge-max` |

보안 키워드가 걸린 탐색 서브에이전트는 위 표의 "탐색·추출"이라도 Hard(opus 이상)로 상향한다 — 이 경우 `explorer-low`를 쓰지 않는다.

tier 에이전트 정의(`.claude/agents/*.md`)는 각 5종을 [SKILL.md](../SKILL.md)의 흐름과 연결한다: `explorer-low`(haiku/low, 읽기전용) · `executor-med`(sonnet/medium) · `executor-high`(sonnet/high) · `reviewer-high`(opus/high, Write/Edit 제외) · `judge-max`(fable/max, Write/Edit 제외, SKILL.md의 fable 강제 트리거 5종 전용).

## 실행 경로 3종

### ① Agent 툴
- 즉석 호출(`subagent_type`을 범용 에이전트로 지정)은 `model`만 지정 가능하고 effort는 지정 불가(기본값 사용).
- **tier 에이전트를 `subagent_type`으로 지정하면 예외다** — `.claude/agents/*.md` frontmatter에 `model`+`effort`가 이미 박혀 있어, Agent 툴 호출만으로 그 조합이 그대로 적용된다(예: `subagent_type: judge-max` → fable/max). effort가 필요한 작업은 우선 tier 에이전트가 있는지부터 확인한다.
- 범용 위임 — 탐색·구현·리뷰 1차 대부분은 이 경로로 충분하다.

### ② Workflow `agent()`
- `model` + `effort`(`'low'`~`'max'`) 모두 지정 가능.
- tier 에이전트로 커버되지 않는 조합, 또는 세션 종류가 Agent 툴을 지원하지 않을 때 이 경로로 effort를 직접 지정한다.

### ③ `codex exec`
- OpenAI 계열 전용 실행 경로. `codex exec -m <모델> -c model_reasoning_effort=<강도>`.
- 네트워크가 필요하면 `-c sandbox_workspace_write.network_access=true`를 추가한다.
- git 조작(커밋·푸시·브랜치 전환)은 샌드박스 제약이 있다 — 위임 전에 해당 세션의 git 권한 범위를 확인한다.

## 포함 안 되는 케이스 폴백 규칙

위 표·tier 에이전트 어느 쪽에도 정확히 맞지 않는 작업이 나오면:

1. **티어 밖 조합** — Workflow `agent()`로 모델·effort를 직접 지정하는 범용 폴백을 쓴다. 5개 tier 에이전트에 없는 모델·effort 조합을 억지로 끼워 맞추지 않는다.
2. **완전히 새로운 유형** — 오류 비용(틀렸을 때 되돌리는 비용) × 탐색 공간(고려할 변수 수)으로 즉석 판정한다. 애매하면 **반올림은 위로**(더 비싼 쪽으로) 한다. 판정 직후 이 표에 행을 수기로 즉시 추가한다 — 다음에 같은 유형이 나오면 재판정하지 않는다.
3. **불확실하고 고위험** — SKILL.md의 fable 강제 트리거 5종에 해당하는지 먼저 확인해 승격하거나, 그래도 애매하면 사용자에게 직접 묻는다. 추측으로 낮은 티어에 배정하지 않는다.
````

### 파일: `.claude/skills/delegate-first/references/prompt-templates.md`

````markdown
# Prompt Templates

## Contents
- Template 1: Read-only investigation
- Template 2: Implementation / edit
- Real examples from this project

## Template 1 — 조사용 (read-only)

```
역할: <한 줄 — 예: "구네식당 프로젝트의 collector R4 선행조건을 조사하는 read-only 조사 에이전트">

작업 디렉터리: <절대경로>
경계: <수정 금지 범위 — 예: "docs/decisions/ 읽기만, 쓰기 금지. 시스템 설정 변경 명령 금지(tccutil·csrutil·systemsetup 등)">

조사 범위: <구체적 질문·파일·경로>
증거 요구: 없다고 결론 내릴 경우 어디까지 찾아봤는지(탐색한 경로·명령) 명시할 것 — "없음"과 "안 찾아봄"을 구분

완료 기준:
- [ ] <검증 가능한 결과물 1>
- [ ] <검증 가능한 결과물 2>
- [ ] 한계·모르는 것 명시

출력 형식: <보고서 구조 — 예: 결론 3줄 + 근거 원문 인용 + 파일:라인 참조>
```

**실제 예시** (`docs/handoff/po-session-handoff.md` §9, 2026-08-13 service 선행조건 조사):

```
역할: ax-template 개선판 설치 상태를 확인하는 read-only 조사 에이전트

작업 디렉터리: /Users/plletdata/Documents/AI/kyjin/goone-rest
경계: service/, docs/decisions/ 읽기만. 파일 수정·git 조작 금지.

조사 범위:
- service/ 현재 상태(파일 존재 여부)
- ~/.claude ax-transform 플러그인 설치 버전
- ~/Documents/AI/kyjin/ai-dev-methodologies 하위 ax-template 레포 상태
- ADR-0010의 선행조건 항목별 충족/미충족 판정

증거 요구: 버전·커밋 SHA는 실제 파일(package.json, .git 등)에서 확인한 원문을 인용. 확인 못한 항목은 "미확인"으로 명시.

완료 기준:
- [ ] ADR-0010 선행조건 표에 충족/미충족 표시
- [ ] 각 판정에 근거 파일 경로 인용
- [ ] service 착수 가능 여부 결론

출력 형식: 표(선행조건 | 상태 | 근거) + 결론 3줄
```

## Template 2 — 구현·편집용

```
역할: <한 줄>

작업 디렉터리: <절대경로, worktree 경로 명시>
경계: <수정 가능 범위 / 수정 금지 범위 — 예: "R1/R2 기존 코드·스키마·마이그레이션 수정 금지">
시스템 설정 명령 금지: 진단·수리 목적이라도 시스템 명령(tccutil, csrutil, Docker 컨테이너 생성/삭제 등) 실행 금지 — 환경 오류 시 중단·보고만.

목표: <1~4개 번호 목록, 구체 산출물>
안전 경계: <위반 시 즉시 중단 조건 — 예: "403/429 즉시 중단(재시도 없음)">

완료 기준 (REPORT.md/응답에 증거 원문):
- [ ] <산출물 1>
- [ ] <산출물 2>
- [ ] 한계·모르는 것 명시 (아는 척 금지)

출력 형식: <파일 경로 + 커밋 여부 + 보고 형식>
```

**실제 예시** (`goone-collector/collector/docs/archive/R4-PO-REQUEST-2026-08-14.md` 발췌):

```
역할: collector R4 — 카카오·네이버 등재 확인으로 매칭표 완성

작업 디렉터리: goone-collector/collector (worktree, 브랜치 feat/collector-r4-kakao-naver)
경계: 기존 R1/R2 코드·스키마·마이그레이션 수정 금지. R4 산출물은 spikes/s0-survey/r4/에 둔다.
시스템 설정 명령 금지: Docker·환경 변경 금지. 환경 오류 시 중단·보고만.

목표:
1. 카카오 Local API로 표본 100곳 등재 여부 확인
2. 네이버 API HUB 지역검색으로 등재 여부 확인
3. matching-table.csv 갱신 + coverage-analysis-r4.md 작성

안전 경계: 403/429 즉시 중단(재시도 없음), 호출 예산 카카오≤500·네이버≤500, 작성자 개인정보 취득 금지.

완료 기준 (REPORT.md에 증거 원문):
- [ ] matching-table.csv 100행 전부 '등재/미등재/보류' 채워짐 ('미확인' 0)
- [ ] 기준축 후보별 커버리지 표 + ADR-0007 권고
- [ ] API 호출량·한도 기록
- [ ] 한계·모르는 것 명시

출력 형식: REPORT.md + PR (git 커밋·push·PR은 PO가 별도 수행 — 이 프로젝트는 PO 소유 규약)
```
````

### 파일: `.claude/agents/explorer-low.md`

````markdown
---
name: explorer-low
description: Read-only exploration and extraction specialist for file search, grep, and simple aggregation. Use for locating code, gathering facts, or summarizing existing content — never for edits or judgment calls.
model: haiku
effort: low
tools: Read, Grep, Glob
---

# Explorer (low)

Find and report — don't modify, don't judge. Read files, grep patterns, glob paths, and summarize what you found with file:line references.

If something isn't found, state what you searched (paths, patterns, commands) rather than declaring it absent. "Not found" and "didn't look" are different answers.

Stay within the working directory and boundaries given in your task. Report facts, not conclusions that require judgment — hand those back to the caller.
````

### 파일: `.claude/agents/executor-med.md`

````markdown
---
name: executor-med
description: Mid-effort implementation specialist for document editing, structuring, and routine implementation work. Use for doc edits, config changes, and well-scoped code changes that follow an existing pattern.
model: sonnet
effort: medium
---

# Executor (medium)

Implement exactly what's scoped — no speculative extras. Match existing file conventions, formatting, and style rather than introducing your own.

Report what you changed with file paths. If you notice something outside scope that needs a decision, flag it — don't decide it yourself.
````

### 파일: `.claude/agents/executor-high.md`

````markdown
---
name: executor-high
description: High-effort implementation specialist for general feature implementation and debugging. Use for multi-file changes, non-trivial logic, or bugs whose root cause isn't obvious yet.
model: sonnet
effort: high
---

# Executor (high)

Implement or debug with the deeper effort this task needs — trace root causes before patching, weigh edge cases, and verify your own change (run tests, reproduce the bug) before reporting done.

Report what changed, why, and the evidence you verified it with (test output, reproduction steps, not just a description). If the real scope turns out larger than expected, say so instead of quietly expanding it.
````

### 파일: `.claude/agents/reviewer-high.md`

````markdown
---
name: reviewer-high
description: High-effort review and verification specialist for code, document, and plan review. Use to review a diff, a plan, or another agent's output for correctness, quality, and missed cases — not to make the edits itself.
model: opus
effort: high
disallowedTools: Write, Edit
---

# Reviewer (high)

Review, don't fix. Read the change or claim under review, reproduce what you can (grep the file, run the test, read the diff), and report findings with severity and evidence — not vague impressions.

Don't trust self-reports at face value: verify the central claim yourself before endorsing it. If you can't verify something, say so explicitly rather than assuming it's fine.
````

### 파일: `.claude/agents/judge-max.md`

````markdown
---
name: judge-max
description: Maximum-effort adjudication specialist for adversarial verification, ADR-level decisions, and legal, financial, permission/authorization, or security judgment calls. Use for the highest-stakes calls where being wrong is expensive — not for routine review.
model: fable
effort: max
disallowedTools: Write, Edit
---

# Judge (max)

This is the last checkpoint before a costly mistake ships. Take the adversarial position: assume the proposal or claim under review is wrong and look for the strongest case against it, not just confirmation of it.

State your judgment plainly (pass / needs changes / reject) with the specific evidence and reasoning behind it. Flag any assumption you had to make due to missing information — don't fill gaps with guesses on a high-stakes call.
````

### 파일: `enforce-subagent-model.cjs` (원본 위치: `~/.claude/hooks/enforce-subagent-model.cjs`, 전역)

````javascript
#!/usr/bin/env node
/**
 * PreToolUse hook (matcher: Agent) — 서브에이전트 모델 라우팅 강제.
 *
 * 정책 (~/.claude/rules/subagent-model-routing.md):
 *   메인 세션이 고비용 모델(Fable/Opus, max 추론)일 때 Agent 호출이 model을
 *   생략하면 세션 모델을 그대로 상속해 토큰이 폭증한다. 따라서 모든 Agent
 *   호출은 목적에 맞는 model을 **명시**해야 한다 (세션 상속 금지).
 *
 * 동작:
 *   - tool_input.model 이 있으면 통과.
 *   - subagent_type 이 자체 model 을 고정한 정의형 에이전트(플러그인 정의에
 *     model frontmatter 보유)는 알 수 없으므로, 알려진 고정 모델 타입 목록은
 *     통과시킨다 (아래 MODEL_PINNED_TYPES).
 *   - 그 외 model 미지정 → exit 2 로 차단하고 라우팅 지침을 돌려준다.
 *   - break-glass(사람 전용): env ALLOW_INHERITED_SUBAGENT_MODEL=1
 */
const MODEL_PINNED_TYPES = new Set([
  // 플러그인 정의에 model이 고정된 타입들 — 상속 위험 없음
  "oh-my-claudecode:writer", // haiku
  "oh-my-claudecode:designer", // sonnet
  "oh-my-claudecode:executor", // sonnet
  "oh-my-claudecode:analyst", // opus
  "oh-my-claudecode:architect", // opus
  "oh-my-claudecode:critic", // opus
  "oh-my-claudecode:planner", // opus
  "statusline-setup",
]);

let raw = "";
process.stdin.on("data", (chunk) => (raw += chunk));
process.stdin.on("end", () => {
  if (process.env.ALLOW_INHERITED_SUBAGENT_MODEL === "1") process.exit(0);
  let input;
  try {
    input = JSON.parse(raw);
  } catch {
    process.exit(0); // 파싱 불가 시 차단하지 않음 (fail-open: 훅 자체 결함으로 작업 정지 방지)
  }
  const toolInput = (input && input.tool_input) || {};
  const model = toolInput.model;
  const subagentType = toolInput.subagent_type || "";
  if (typeof model === "string" && model.trim()) process.exit(0);
  if (MODEL_PINNED_TYPES.has(subagentType)) process.exit(0);
  process.stderr.write(
    [
      "[subagent-model-routing] Agent 호출에 model 명시가 없습니다 — 세션 모델 상속 금지.",
      "메인 세션은 결정/방향/판정 전용입니다. 실행 위임 시 목적에 맞는 model을 명시하세요:",
      "  - 탐색/파일검색/단순집계/기계적 편집: model: 'haiku' 또는 'sonnet'",
      "  - 일반 구현/디버깅/리뷰 1차: model: 'sonnet'",
      "  - 적대판정/돈/권한/동시성/release-gate/보안: model: 'opus'",
      "model 파라미터를 추가해 다시 호출하세요.",
    ].join("\n")
  );
  process.exit(2);
});
````

### 파일: `subagent-model-routing.md` (원본 위치: `~/.claude/rules/subagent-model-routing.md`, 전역)

````markdown
# 서브에이전트 모델 라우팅 강제 (전 프로젝트, 2026-07-16 사용자 확정)

메인 세션이 고비용 모델(Fable 5 / Opus, max 추론)일 때의 **강제 규칙**:

## 원칙
1. **메인 세션은 결정·방향·판정 종합 전용이다.** 구현, 테스트 실행/수정, 탐색,
   문서 초안 등 실행 작업은 서브에이전트에 위임한다.
2. **모든 Agent(서브에이전트) 호출은 `model`을 명시한다 — 세션 모델 상속 금지.**
   추론강도(effort)를 지원하는 경로(Workflow `agent()` 등)에서는 effort도 명시한다.
3. 라우팅 기준 (글로벌 CLAUDE.md OMO 정책과 동일):
   - 탐색·파일검색·단순집계·기계적 편집 → `haiku`/`sonnet`
   - 일반 구현·보통 디버깅·리뷰 1차·문서 구조화 → `sonnet`
   - 적대판정·batch-audit·돈·권한·동시성·release-gate·보안 → `opus`
   - 라벨링·생성 "품질" 작업은 다운시프트 금지 (2026-07-15 확정 유지)
4. 판정·종합·보고는 메인이 한다. 서브에이전트 결과를 메인이 재검하지 않고
   그대로 신뢰하지 말 것(독립 검증 게이트는 별도 opus 에이전트).

## 강제 장치
- PreToolUse 훅 `~/.claude/hooks/enforce-subagent-model.cjs` (matcher: Agent)가
  `model` 미지정 호출을 차단한다 (자체 model 고정 에이전트 타입은 예외 목록).
- break-glass는 사람 전용: `ALLOW_INHERITED_SUBAGENT_MODEL=1`.
  Claude는 이 변수를 스스로 설정하지 않는다.

## 근거
- 2026-07-16 사용자 지시: "메인은 fable5 max 추론이라 엄청난 토큰 사용 —
  결정사항과 방향결정만 메인에서, 나머지는 모두 서브에이전트에 위임하고
  모델·추론강도를 목적에 맞게. 세션 상속 금지. 정책을 문서화하고 강제 구현."
````

---

## 4. 프로젝트 종속 부분 (이식 시 치환 필요)

- **delegation log 경로**: `SKILL.md`의 3단계 체크리스트 항목이 `docs/handoff/delegation-log.md`를 위임 로그의 append 대상으로 하드코딩하고 있다. 새 레포에서 그대로 쓰려면 이 경로에 파일을 새로 만들어야 하고, 다른 경로를 쓰려면 `SKILL.md`의 해당 문구(3번 체크리스트 항목)를 직접 고쳐야 한다. 여러 프로젝트에서 재사용할 계획이면 경로를 프로젝트별로 파라미터화(예: 스킬 설정값이나 CLAUDE.md 오버라이드)하는 편이 낫다 — 현재는 문자열로 박혀 있다.
- **goone 고유 언급**: `references/prompt-templates.md`의 "실제 예시" 2건(조사용 템플릿의 ax-template 설치 확인 예시, 구현용 템플릿의 collector R4 카카오·네이버 매칭 예시)은 goone-rest 프로젝트의 실제 작업에서 가져온 것이다. 절대경로(`/Users/plletdata/Documents/AI/kyjin/goone-rest`), ADR 번호(ADR-0007, ADR-0010), 워크트리 이름(`goone-collector/collector`) 등이 그대로 박혀 있어 새 레포 컨텍스트에는 의미가 없다. 템플릿의 **구조**(역할/작업 디렉터리/경계/목표/완료기준/출력형식 필드)는 그대로 재사용하되, **예시 본문**은 새 프로젝트의 실제 사례로 교체하거나 삭제할 것.
- **훅 파일(enforce-subagent-model.cjs)**: 프로젝트 경로 하드코딩 없음 — 그대로 재사용 가능. 다만 원본이 `~/.claude/hooks/`(전역)에 있다는 점은 새 레포 설치 시 결정 사항(§2 참고).
- **글로벌 규칙(subagent-model-routing.md)**: 전 프로젝트 공통으로 작성돼 있어 프로젝트 종속 내용 없음 — 그대로 새 레포의 전역 또는 로컬 규칙으로 채택 가능.
- **routing-matrix.md, tier 에이전트 5종**: 프로젝트 종속 언급 없음 — 그대로 재사용 가능.
- **구 명칭 잔존 참조 검색**: `grep -r delegating-execution` — 개명(§명칭 확정) 이후 이식 대상 트리 전체에서 구 명칭이 남아있지 않은지 확인한다.

---

## 5. 운영 학습 (2026-08-17 실전 — 새 레포 개선 백로그)

- Agent 즉석 호출(subagent_type을 범용 에이전트로 지정하는 방식)은 effort를 지정할 수 없다 — effort를 통제하려면 tier 에이전트를 `subagent_type`으로 직접 지정하거나 Workflow `agent()`/`codex exec` 경로를 써야 한다. 오늘 구현 레인 4건이 이를 놓쳐 opus/(기본 effort)로 나갔다.
- 위임 로그 단계가 오늘 SKILL.md 체크리스트 3번에 새로 추가됐다(`agent/role/model/effort/path` 스키마로 기록). 아직 이 handoff 패키지 안에는 로그 스키마를 강제하는 코드는 없다 — 문서 지침으로만 존재.
- 판정 에이전트(reviewer-high, judge-max 등)가 보고 없이 idle 상태로 남는 패턴이 잦았다 — SendMessage로 판정문을 재요구해야 하는 관행이 필요하다는 것이 오늘 확인됐다.
- fable 강제 트리거 5종(SKILL.md §"Mandatory top-tier-model triggers")이 오늘 8회 실전 검증됐다 — 그중 2회는 REFUTED(반박) 발견이 나왔고, 같은 라운드 안에서 봉합·수렴됐다.

---

## 6. 검증 방법 (새 레포에서)

1. **스킬 로드 확인**: 새 레포 세션에서 `/delegate-first`를 호출해 `SKILL.md` 본문이 그대로 로드되는지 확인한다.
2. **훅 차단 스모크**: `model` 파라미터 없이 Agent 툴을 즉석 호출해본다 — `enforce-subagent-model.cjs`가 exit 2로 차단하고 라우팅 안내 메시지를 stderr로 반환하는지 확인한다.
3. **tier 에이전트 스폰 1회**: `subagent_type: explorer-low`(또는 다른 tier)로 Agent를 1회 호출해 frontmatter의 `model`+`effort` 조합(예: haiku/low)이 실제로 적용되는지 확인한다.
